# Sphinx core
sphinx>=7.0
sphinx-rtd-theme>=2.0
myst-parser>=1.0
sphinx-autoapi>=3.0
sphinx-autodoc-typehints>=1.25

# For cross-linking to standard libs
numpy>=1.0
packaging>=23.0

# Install your own package for import resolution
-e ../   # adjust path so RTD can import xboinc during build
